<UserControl x:Class="MVVM_Examples.View.SingleItemView.SingleItemView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:MVVM_Examples.View.SingleItemView"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.DataContext>
        <local:SingleItemViewModel/>
    </UserControl.DataContext>

    <StackPanel>
        <Label Content="{Binding current_Object.Name}"></Label>
        <Label Content="{Binding current_Object.Weight}"></Label>
        <Label Content="{Binding current_Object.ID}"></Label>
        <Button Content="Previous" Command="{Binding PrevCommand}"></Button>
        <Button Content="Next" Command="{Binding NextCommand}"></Button>
    </StackPanel>
</UserControl>
